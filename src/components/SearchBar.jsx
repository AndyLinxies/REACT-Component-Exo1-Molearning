import React from 'react';

const Search = () => {
    return (
        <div className="search">
            <span>Search :</span>
            <input type="text" />
            <button >Go</button>
        </div>
    );
};

export default Search;